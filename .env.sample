OPENAI_API_KEY=your_api_key

# For Docker Compose (default)
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/realtime_agents
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NODE_ENV=development

# For local development (if running without Docker)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/realtime_agents

# For production deployment
# DATABASE_URL=your_production_database_url
# NEXT_PUBLIC_BASE_URL=https://your-app-domain.com